export enum AccountType {
  SAVINGS = 0,
  CHECKING = 1,
}

export enum PixKeyType {
  CPF = 0,
  CNPJ = 1,
  EMAIL = 2,
  CELLPHONE = 3,
  RANDOM = 4,
}

export enum PaymentMethod {
  PIX = 911,
  BANK_SLIP = 822,
}
